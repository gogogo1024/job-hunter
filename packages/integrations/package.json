{
  "name": "@job-hunter/integrations",
  "private": true,
  "engines": {
    "node": ">=24"
  },
  "type": "module",
  "scripts": { "build": "tsc -b tsconfig.build.json", "dev": "tsc -b tsconfig.build.json --watch", "typecheck": "tsc --noEmit", "test": "vitest" },
  "devDependencies": {
    "vitest": "latest"
  },
  "dependencies": {
    "@job-hunter/shared": "workspace:*",
    "@job-hunter/schema": "workspace:*"
  },
  "exports": { ".": "./dist/index.js" },
  "types": "./dist/index.d.ts"
}
